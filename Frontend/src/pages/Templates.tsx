import { useCallback, useEffect, useMemo, useState } from "react";
import { Copy, FileText, Flag, Pencil, Plus, RefreshCw, Search, Tag, Trash2, RotateCcw } from "lucide-react";
import { useNavigate } from "react-router-dom";
import { Button } from "@/components/ui/button";
import { ConfirmationDialog } from "@/components/ui/confirmation-dialog";
import { Input } from "@/components/ui/input";
import { Sheet, SheetContent, SheetDescription, SheetTitle } from "@/components/ui/sheet";
import { useAuth } from "@/contexts/AuthContext";
import { ApiError, apiRequest } from "@/lib/api";
import { getActiveMembership } from "@/lib/workspace";
import { cn } from "@/lib/utils";
import { MetaTemplateLibrary } from "@/pages/MetaTemplateLibrary";

type TemplateTab = "library" | "active" | "meta" | "deleted";
type TemplateStatus = "DRAFT" | "PENDING" | "APPROVED" | "REJECTED" | "DELETING" | "DELETE_FAILED" | "DELETED";
type TemplateRecord = { id: string; name: string; key: string; status: TemplateStatus; deletionError?: string | null; deletionAttemptCount?: number; metaStatus?: string | null; metaRejectionReason?: string | null; metaTemplateId?: string | null; category: string; language: string; templateType: string; headerType?: string; headerText?: string | null; headerFileName?: string | null; body: string; footer: string | null; content?: { buttons?: string[]; websiteUrl?: string } | null; createdBy: string; createdAt: string; updatedAt: string };
type TemplateListResponse = { items: TemplateRecord[]; pagination: { page: number; pageSize: number; total: number; totalPages: number } };
type TemplateSyncResponse = { imported: number; wabaId: string; debug?: { tokenSource?: string; pages?: number; remoteCount?: number; importedCount?: number; categories?: Record<string, number> } };
const tabs: Array<{ id: TemplateTab; label: string }> = [{ id: "library", label: "My Templates" }, { id: "active", label: "Active" }, { id: "meta", label: "Meta Template Library" }, { id: "deleted", label: "Deleted" }];

function TemplatesHeader({ onCreate }: { onCreate: () => void }) { return <header data-testid="templates-page-header" className="flex-none border-b border-[var(--border)] bg-white"><div className="mx-auto flex min-h-[78px] max-w-[1400px] items-center justify-between gap-4 px-5 py-3 sm:px-8"><div className="flex min-w-0 items-center gap-4"><div className="flex size-10 shrink-0 items-center justify-center rounded-full bg-[var(--brand)] text-white shadow-[0_3px_8px_rgba(17,107,111,.15)]"><FileText size={19} /></div><div className="min-w-0"><h1 className="text-[17px] font-semibold leading-tight text-[var(--text-primary)]">Templates</h1><div className="mt-0.5 text-[12px] text-[var(--text-secondary)]">Managing WhatsApp Templates</div></div></div><Button onClick={onCreate} className="h-10 shrink-0 bg-[var(--brand)] px-4 text-[14px] hover:bg-[var(--brand-hover)]"><Plus size={17} /> New Template</Button></div></header>; }
function TemplateTabs({ activeTab, onChange }: { activeTab: TemplateTab; onChange: (tab: TemplateTab) => void }) { return <div role="tablist" aria-label="Template views" className="flex min-h-[54px] overflow-hidden rounded-md border border-[var(--border)] bg-white">{tabs.map((tab) => <button key={tab.id} type="button" role="tab" aria-selected={activeTab === tab.id} onClick={() => onChange(tab.id)} className={cn("relative flex min-w-[140px] flex-1 items-center justify-center px-4 text-[13px] text-[var(--text-secondary)] transition-colors hover:bg-[var(--brand-soft)] sm:min-w-0", activeTab === tab.id && "bg-[var(--brand-soft)] text-[var(--brand)] after:absolute after:inset-x-0 after:bottom-0 after:h-0.5 after:bg-[var(--brand)]")}>{tab.label}</button>)}</div>; }
function TemplateToolbar({ query, onQueryChange, status, onStatusChange, category, onCategoryChange, onSync, syncing }: { query: string; onQueryChange: (value: string) => void; status: string; onStatusChange: (value: string) => void; category: string; onCategoryChange: (value: string) => void; onSync: () => void; syncing: boolean }) {
  return <div data-testid="templates-filter-toolbar" className="flex flex-wrap items-center gap-3"><div className="relative"><Search className="pointer-events-none absolute left-2.5 top-2.5 size-4 text-[var(--text-primary)]" /><Input aria-label="Search templates" value={query} onChange={(event) => onQueryChange(event.target.value)} placeholder="Search a template by name" className="h-10 w-[280px] pl-9 text-[13px]" /></div><label className="flex h-10 items-center gap-1.5 px-2 text-[13px] text-[var(--text-secondary)]"><Flag size={15} /><span>Status</span><select aria-label="Filter templates by status" value={status} onChange={(event) => onStatusChange(event.target.value)} className="h-8 rounded border border-[var(--border)] bg-white px-2 text-[12px] text-[var(--text-primary)] outline-none focus:border-[var(--brand)]"><option value="">All</option><option value="DRAFT">Draft</option><option value="PENDING">Pending</option><option value="APPROVED">Approved</option><option value="REJECTED">Rejected</option></select></label><label className="flex h-10 items-center gap-1.5 px-2 text-[13px] text-[var(--text-secondary)]"><Tag size={15} /><span>Category</span><select aria-label="Filter templates by category" value={category} onChange={(event) => onCategoryChange(event.target.value)} className="h-8 rounded border border-[var(--border)] bg-white px-2 text-[12px] text-[var(--text-primary)] outline-none focus:border-[var(--brand)]"><option value="">All</option><option value="Marketing">Marketing</option><option value="Utility">Utility</option><option value="Authentication">Authentication</option></select></label><Button variant="outline" size="sm" onClick={onSync} disabled={syncing} className="ml-auto h-10 border-[var(--brand)]/25 bg-[var(--brand-soft)] text-[var(--brand)] hover:bg-[var(--brand-soft)]"><RefreshCw className={cn(syncing && "animate-spin")} size={17} /> {syncing ? "Syncing from Meta" : "Sync from Meta"}</Button></div>;
}
function statusLabel(status: TemplateStatus) { return status === "DELETING" ? "Deleting" : status === "DELETE_FAILED" ? "Delete failed" : status.charAt(0) + status.slice(1).toLowerCase(); }

function ActiveTemplatesTable({ templates, onOpen, onEdit, onDelete, onRestore }: { templates: TemplateRecord[]; onOpen: (template: TemplateRecord) => void; onEdit: (template: TemplateRecord) => void; onDelete: (template: TemplateRecord) => void; onRestore: (template: TemplateRecord) => void }) {
  return <section data-testid="templates-table-panel" className="flex min-h-0 flex-1 flex-col overflow-hidden rounded-md border border-[var(--border)] bg-white"><div data-testid="templates-table-scroll-region" className="min-h-0 flex-1 overflow-auto"><table className="w-full min-w-[900px] border-collapse text-left"><thead className="sticky top-0 z-10 bg-[#fafbfd] text-[13px] font-semibold text-[var(--text-primary)]"><tr className="border-b border-[var(--border)]"><th className="px-6 py-4">Template Name</th><th className="px-5 py-4">Status</th><th className="px-5 py-4">Category</th><th className="px-5 py-4">Language(s)</th><th className="px-5 py-4">Created By</th><th className="w-28 px-5 py-4"><span className="sr-only">Actions</span></th></tr></thead><tbody>{templates.map((template) => <tr key={template.id} tabIndex={0} onClick={() => onOpen(template)} onKeyDown={(event) => { if (event.key === "Enter" || event.key === " ") { event.preventDefault(); onOpen(template); } }} className="group cursor-pointer border-b border-[var(--border-soft)] text-[14px] last:border-b-0 hover:bg-[var(--brand-soft)]/30 focus-visible:bg-[var(--brand-soft)]/30"><td className="px-6 py-4"><div className="font-medium text-[var(--text-primary)]">{template.name}</div><div className="mt-0.5 flex items-center gap-1 text-[12px] text-[var(--text-muted)]">{template.key}<button type="button" aria-label={`Copy ${template.key}`} onClick={(event) => { event.stopPropagation(); void navigator.clipboard?.writeText(template.key); }} className="text-[var(--text-muted)] hover:text-[var(--brand)]"><Copy size={12} /></button>{template.metaTemplateId && <span className="rounded-full bg-[var(--brand-soft)] px-1.5 py-0.5 text-[9px] text-[var(--brand)]">Meta</span>}</div></td><td className="px-5 py-4"><span className="inline-flex items-center gap-2 text-[var(--text-primary)]"><span className={cn("size-2 rounded-full", template.status === "APPROVED" ? "bg-[var(--success)]" : template.status === "REJECTED" || template.status === "DELETE_FAILED" || template.status === "DELETED" ? "bg-[var(--danger)]" : "bg-[var(--warning)]")} />{statusLabel(template.status)}{template.metaStatus && template.metaStatus !== template.status && <span className="text-[10px] text-[var(--text-muted)]">({template.metaStatus.toLowerCase().replace(/_/g, " ")})</span>}</span>{template.deletionError && <div className="mt-1 max-w-[260px] truncate text-[10px] text-[var(--danger)]" title={template.deletionError}>{template.deletionError}</div>}</td><td className="px-5 py-4">{template.category}</td><td className="px-5 py-4">{template.language}</td><td className="px-5 py-4">{template.createdBy}</td><td className="px-5 py-4"><div data-testid={`template-row-actions-${template.key}`} className="flex items-center gap-1 rounded-md border border-[var(--border)] bg-white px-1 py-1 opacity-0 transition-opacity group-hover:opacity-100 group-focus-within:opacity-100"><button type="button" aria-label={`Edit ${template.name}`} onClick={(event) => { event.stopPropagation(); onEdit(template); }} className="flex size-7 items-center justify-center rounded text-[var(--text-secondary)] hover:bg-[var(--brand-soft)] hover:text-[var(--brand)]" disabled={template.status === "DELETING"}><Pencil size={15} /></button>{template.status === "DELETED" ? <button type="button" aria-label={`Restore ${template.name}`} onClick={(event) => { event.stopPropagation(); onRestore(template); }} className="flex size-7 items-center justify-center rounded text-[var(--text-secondary)] hover:bg-[var(--brand-soft)] hover:text-[var(--brand)]"><RotateCcw size={15} /></button> : template.status === "DELETING" ? <span className="px-2 text-[11px] text-[var(--text-muted)]">Deleting…</span> : <button type="button" aria-label={`${template.status === "DELETE_FAILED" ? "Retry delete" : "Delete"} ${template.name}`} onClick={(event) => { event.stopPropagation(); onDelete(template); }} className="flex size-7 items-center justify-center rounded text-[var(--text-secondary)] hover:bg-red-50 hover:text-[var(--danger)]"><Trash2 size={15} /></button>}</div></td></tr>)}</tbody></table>{templates.length === 0 && <div className="flex min-h-[220px] items-center justify-center text-[13px] text-[var(--text-secondary)]">No templates found.</div>}</div><div className="flex flex-none items-center justify-between border-t border-[var(--border-soft)] px-6 py-3 text-[12px] text-[var(--text-secondary)]"><span>Total templates: <strong className="text-[var(--text-primary)]">{templates.length}</strong></span><span>Showing {templates.length} of {templates.length}</span></div></section>;
}

function TemplateDetailsDrawer({ template, onOpenChange, onEdit, onUse }: { template: TemplateRecord | null; onOpenChange: (open: boolean) => void; onEdit: (template: TemplateRecord) => void; onUse: (template: TemplateRecord) => void }) {
  const open = Boolean(template);
  if (!template) return <Sheet open={false} onOpenChange={onOpenChange}><SheetContent /></Sheet>;
  const buttons = template.content?.buttons ?? [];
  return <Sheet open={open} onOpenChange={onOpenChange}><SheetContent side="right" closeLabel="Close template details" className="inset-y-0 right-0 flex h-full w-full max-w-[480px] flex-col overflow-hidden sm:max-w-[480px]"><div className="flex-none border-b border-[var(--border-soft)] px-5 py-5 pr-16"><div className="flex items-center gap-2"><span className={cn("size-2 rounded-full", template.status === "APPROVED" ? "bg-[var(--success)]" : template.status === "REJECTED" || template.status === "DELETE_FAILED" || template.status === "DELETED" ? "bg-[var(--danger)]" : "bg-[var(--warning)]")} /><span className="text-[11px] font-medium uppercase tracking-[0.08em] text-[var(--text-muted)]">{statusLabel(template.status)}</span></div><SheetTitle className="mt-2 text-lg font-medium text-[var(--text-primary)]">{template.name}</SheetTitle><SheetDescription className="mt-1 text-xs text-[var(--text-muted)]">Template details and review information</SheetDescription></div><div className="min-h-0 flex-1 overflow-y-auto px-5 py-5"><div className="grid grid-cols-2 gap-x-4 gap-y-4 border-b border-[var(--border-soft)] pb-5 text-xs"><DetailItem label="Template key" value={template.key} /><DetailItem label="Category" value={template.category} /><DetailItem label="Language" value={template.language} /><DetailItem label="Type" value={template.templateType} /><DetailItem label="Created by" value={template.createdBy} /><DetailItem label="Created" value={new Intl.DateTimeFormat("en-IN", { day: "2-digit", month: "short", year: "numeric" }).format(new Date(template.createdAt))} />{template.metaTemplateId && <DetailItem label="Meta template ID" value={template.metaTemplateId} />}</div><section className="border-b border-[var(--border-soft)] py-5"><h3 className="text-xs font-medium uppercase tracking-[0.08em] text-[var(--text-muted)]">Message preview</h3><div className="mt-3 overflow-hidden rounded-lg border border-[#cddbd2] bg-[#e7f0e9] p-3 text-sm leading-5 text-[#27332e]"><div className="rounded-lg rounded-tl-none bg-white px-3 py-2.5 shadow-[0_1px_2px_rgba(0,0,0,.08)]">{template.headerText && <div className="mb-2 font-medium">{template.headerText}</div>}<div className="whitespace-pre-wrap">{template.body}</div>{template.footer && <div className="mt-2 text-xs text-[#84918a]">{template.footer}</div>}{buttons.length > 0 && <div className="mt-3 space-y-1.5">{buttons.map((button) => <div key={button} className="rounded border border-[#d9e4dc] px-2 py-1.5 text-center text-xs font-medium text-[var(--brand)]">{button === "website" ? "Visit Website" : button.replace(/-/g, " ")}</div>)}</div>}</div></div></section>{template.metaRejectionReason && <section data-testid="template-rejection-reason" className="border-b border-[var(--border-soft)] py-5"><h3 className="text-xs font-medium uppercase tracking-[0.08em] text-[var(--danger)]">Rejection reason</h3><div className="mt-3 whitespace-pre-wrap rounded-md border border-red-100 bg-red-50 px-3 py-3 text-xs leading-5 text-[var(--danger)]">{template.metaRejectionReason}</div></section>}<section className="py-5"><h3 className="text-xs font-medium uppercase tracking-[0.08em] text-[var(--text-muted)]">Review status</h3><p className="mt-2 text-xs leading-5 text-[var(--text-secondary)]">{template.status === "REJECTED" ? "Update this template and submit it again when the issue is resolved." : template.status === "APPROVED" ? "This template is approved and ready to use." : template.status === "PENDING" ? "This template is waiting for Meta review." : template.status === "DELETING" ? "Deletion is in progress. Meta will be updated in the background." : template.status === "DELETE_FAILED" ? `Meta deletion failed after several attempts. Retry deletion${template.deletionError ? `: ${template.deletionError}` : "."}` : template.status === "DELETED" ? "This template has been deleted from Meta." : "This template is saved in your workspace."}</p></section></div><div className="flex flex-none items-center justify-end gap-2 border-t border-[var(--border-soft)] bg-white px-5 py-4">{template.status === "APPROVED" && <Button onClick={() => onUse(template)} className="h-9 bg-[var(--brand)] text-xs hover:bg-[var(--brand-hover)]">Use in campaign</Button>}<Button variant="outline" onClick={() => onEdit(template)} disabled={template.status === "DELETING" || template.status === "DELETE_FAILED"} className="h-9 text-xs">Edit template</Button></div></SheetContent></Sheet>;
}

function DetailItem({ label, value }: { label: string; value: string }) { return <div><div className="text-[11px] text-[var(--text-muted)]">{label}</div><div className="mt-1 break-words text-xs font-medium capitalize text-[var(--text-primary)]">{value}</div></div>; }
export function Templates() {
  const navigate = useNavigate();
  const { accessToken, user } = useAuth();
  const workspaceId = getActiveMembership(user)?.workspace.id;
  const [activeTab, setActiveTab] = useState<TemplateTab>("library");
  const [query, setQuery] = useState("");
  const [statusFilter, setStatusFilter] = useState("");
  const [categoryFilter, setCategoryFilter] = useState("");
  const [templates, setTemplates] = useState<TemplateRecord[]>([]);
  const [loading, setLoading] = useState(true);
  const [syncing, setSyncing] = useState(false);
  const [error, setError] = useState("");
  const [syncResult, setSyncResult] = useState("");
  const [templateToDelete, setTemplateToDelete] = useState<TemplateRecord | null>(null);
  const [templateDetails, setTemplateDetails] = useState<TemplateRecord | null>(null);
  const loadTemplates = useCallback(async (showSync = false) => {
    if (!workspaceId || !accessToken) { setTemplates([]); setLoading(false); return; }
    if (showSync) setSyncing(true); else setLoading(true);
    setError("");
    try { const result = await apiRequest<TemplateListResponse>(`/workspaces/${workspaceId}/templates?status=all&search=${encodeURIComponent(query)}`, { headers: { authorization: `Bearer ${accessToken}` } }); setTemplates(result.items); }
    catch (caughtError) { setError(caughtError instanceof ApiError ? caughtError.message : "Unable to load templates."); }
    finally { setLoading(false); setSyncing(false); }
  }, [accessToken, query, workspaceId]);
  const syncTemplates = async () => {
    if (!workspaceId || !accessToken || syncing) return;
    setSyncing(true);
    setError("");
    setSyncResult("");
    try {
      const result = await apiRequest<TemplateSyncResponse>(`/workspaces/${workspaceId}/templates/sync`, { method: "POST", headers: { authorization: `Bearer ${accessToken}` } });
      const categories = Object.entries(result.debug?.categories ?? {}).map(([category, count]) => `${category}: ${count}`).join(", ");
      setSyncResult(result.imported === 0
        ? `Meta returned 0 templates. WABA ${result.wabaId}; token: ${result.debug?.tokenSource ?? "unknown"}; pages: ${result.debug?.pages ?? 0}. Verify this WABA owns the templates and the token has whatsapp_business_management.`
        : `Synced ${result.imported} template${result.imported === 1 ? "" : "s"} from Meta. WABA ${result.wabaId}; token: ${result.debug?.tokenSource ?? "unknown"}; pages: ${result.debug?.pages ?? 0}${categories ? `; ${categories}` : ""}.`);
      await loadTemplates();
    } catch (caughtError) {
      if (caughtError instanceof ApiError) {
        const details = caughtError.details && typeof caughtError.details === "object" && !Array.isArray(caughtError.details) ? caughtError.details as Record<string, unknown> : {};
        const debug = [
          typeof details.wabaId === "string" ? `WABA ${details.wabaId}` : "",
          typeof details.tokenSource === "string" ? `token: ${details.tokenSource}` : "",
          typeof details.providerCode === "number" ? `Meta code: ${details.providerCode}` : "",
          typeof details.providerSubcode === "number" ? `Meta subcode: ${details.providerSubcode}` : "",
          typeof details.diagnosis === "string" ? details.diagnosis : "",
        ].filter(Boolean).join("; ");
        setError(`${caughtError.message}${debug ? ` [Debug: ${debug}]` : ""}`);
      } else {
        setError("Unable to sync templates from Meta.");
      }
    } finally {
      setSyncing(false);
    }
  };
  useEffect(() => { void loadTemplates(); }, [loadTemplates]);
  const visibleTemplates = useMemo(() => { const matches = templates.filter((template) => (template.name.toLowerCase().includes(query.toLowerCase()) || template.key.toLowerCase().includes(query.toLowerCase())) && (!statusFilter || template.status === statusFilter) && (!categoryFilter || template.category === categoryFilter)); if (activeTab === "deleted") return matches.filter((template) => ["DELETING", "DELETE_FAILED", "DELETED"].includes(template.status)); return matches.filter((template) => !["DELETING", "DELETE_FAILED", "DELETED"].includes(template.status)); }, [activeTab, categoryFilter, query, statusFilter, templates]);
  const updateStatus = async (template: TemplateRecord, action: "delete" | "restore") => { if (!workspaceId || !accessToken) return; try { await apiRequest(action === "delete" ? `/workspaces/${workspaceId}/templates/${template.id}` : `/workspaces/${workspaceId}/templates/${template.id}/restore`, { method: action === "delete" ? "DELETE" : "POST", headers: { authorization: `Bearer ${accessToken}` } }); await loadTemplates(true); } catch (caughtError) { setError(caughtError instanceof ApiError ? caughtError.message : "Unable to update this template."); } };
  return <><div data-testid="templates-page" className="flex h-full min-h-0 flex-col overflow-hidden bg-white"><TemplatesHeader onCreate={() => navigate("/createtemplate")} /><main className="min-h-0 flex-1 overflow-hidden bg-white"><div className="mx-auto flex h-full min-h-0 max-w-[1400px] flex-col gap-4 px-5 py-4 sm:px-8"><TemplateTabs activeTab={activeTab} onChange={setActiveTab} />{activeTab === "meta" ? <MetaTemplateLibrary workspaceId={workspaceId} accessToken={accessToken} onAdded={() => { setActiveTab("library"); void loadTemplates(); }} /> : <><TemplateToolbar query={query} onQueryChange={setQuery} status={statusFilter} onStatusChange={setStatusFilter} category={categoryFilter} onCategoryChange={setCategoryFilter} onSync={() => void syncTemplates()} syncing={syncing} />{syncResult && <div role="status" className="rounded-md border border-[var(--brand)]/20 bg-[var(--brand-soft)] px-4 py-3 text-[12px] text-[var(--brand)]">{syncResult}</div>}{error && <div role="alert" className="rounded-md border border-red-100 bg-red-50 px-4 py-3 text-[12px] text-[var(--danger)]">{error}</div>}{loading ? <div className="flex min-h-0 flex-1 items-center justify-center rounded-md border border-[var(--border)] bg-white text-[13px] text-[var(--text-secondary)]">Loading templates...</div> : <ActiveTemplatesTable templates={visibleTemplates} onOpen={setTemplateDetails} onEdit={(template) => navigate(`/createtemplate?templateId=${template.id}`)} onDelete={setTemplateToDelete} onRestore={(template) => void updateStatus(template, "restore")} />}</>}</div></main></div><ConfirmationDialog open={Boolean(templateToDelete)} onOpenChange={(open) => { if (!open) setTemplateToDelete(null); }} title={templateToDelete?.status === "DELETE_FAILED" ? "Retry template deletion?" : "Delete this template?"} description={templateToDelete?.status === "DELETE_FAILED" ? `The template “${templateToDelete.name}” will be queued for deletion again.` : `The template “${templateToDelete?.name ?? ""}” will be moved to Deleted and removed from Meta in the background.`} confirmLabel={templateToDelete?.status === "DELETE_FAILED" ? "Retry deletion" : "Delete template"} pendingLabel="Deleting..." tone="danger" onConfirm={() => templateToDelete ? updateStatus(templateToDelete, "delete") : Promise.resolve()} /><TemplateDetailsDrawer template={templateDetails} onOpenChange={(open) => { if (!open) setTemplateDetails(null); }} onEdit={(template) => { setTemplateDetails(null); navigate(`/createtemplate?templateId=${template.id}`); }} onUse={(template) => { setTemplateDetails(null); navigate(`/campaigns?templateKey=${encodeURIComponent(template.key)}`); }} /></>;
}
