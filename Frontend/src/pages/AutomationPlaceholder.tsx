import { ListChecks, Settings, Workflow } from "lucide-react";
import { AutomationShell } from "@/components/automation/AutomationShell";
const icons = { workflows: Workflow, sequences: ListChecks, settings: Settings } as const;
export function AutomationPlaceholder({ kind }: { kind: keyof typeof icons }) { const Icon = icons[kind]; const title = kind === "workflows" ? "Workflows" : kind === "sequences" ? "Sequences" : "Automation Settings"; return <AutomationShell><div data-testid={`automation-${kind}-page`} className="flex h-full items-center justify-center bg-[var(--page-background)] p-6"><section className="max-w-md rounded-md border border-[var(--border-soft)] bg-white p-8 text-center shadow-[0_3px_12px_rgba(30,40,55,.045)]"><Icon className="mx-auto text-[var(--brand)]" size={30} /><h1 className="mt-4 text-lg font-semibold text-[var(--text-primary)]">{title}</h1><div className="mt-2 text-sm leading-6 text-[var(--text-secondary)]">This section is ready for the next automation module. Automations can already be managed from the navigation.</div></section></div></AutomationShell>; }
