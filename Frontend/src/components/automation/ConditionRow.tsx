import { Trash2 } from "lucide-react";
import { Input } from "@/components/ui/input";
import type { AutomationCondition, AutomationOperator } from "@/types/automation";
import type { AutomationTriggerDefinition } from "@/config/automation-triggers";
const operators: Array<{ value: AutomationOperator; label: string }> = [{ value: "equals", label: "Equals" }, { value: "not_equals", label: "Does not equal" }, { value: "contains", label: "Contains" }, { value: "not_contains", label: "Does not contain" }, { value: "starts_with", label: "Starts with" }, { value: "ends_with", label: "Ends with" }, { value: "is_empty", label: "Is empty" }, { value: "is_not_empty", label: "Is not empty" }, { value: "greater_than", label: "Greater than" }, { value: "less_than", label: "Less than" }, { value: "is", label: "Is" }, { value: "is_not", label: "Is not" }, { value: "is_any_of", label: "Is any of" }, { value: "is_none_of", label: "Is none of" }];
const selectClass = "h-9 w-full rounded-md border border-[var(--border)] bg-white px-2.5 text-xs text-[var(--text-primary)] outline-none focus:border-[var(--brand)]";
export function ConditionRow({ condition, definition, onChange, onRemove }: { condition: AutomationCondition; definition?: AutomationTriggerDefinition; onChange: (condition: AutomationCondition) => void; onRemove: () => void }) { const field = definition?.fields.find((item) => item.value === condition.field); const noValue = condition.operator === "is_empty" || condition.operator === "is_not_empty"; return <div className="flex flex-col gap-2 rounded-md border border-[var(--border-soft)] bg-[#fafbfc] p-3 lg:flex-row lg:items-center"><select aria-label="Condition field" className={selectClass} value={condition.field} onChange={(event) => onChange({ ...condition, field: event.target.value })}>{definition?.fields.map((item) => <option key={item.value} value={item.value}>{item.label}</option>)}</select><select aria-label="Condition operator" className={selectClass} value={condition.operator} onChange={(event) => onChange({ ...condition, operator: event.target.value as AutomationOperator, ...(event.target.value === "is_empty" || event.target.value === "is_not_empty" ? { value: undefined } : {}) })}>{operators.map((item) => <option key={item.value} value={item.value}>{item.label}</option>)}</select>{noValue ? <div className="h-9 w-full text-xs text-[var(--text-muted)] lg:pt-2">No value needed</div> : <Input aria-label="Condition value" value={condition.value == null ? "" : String(condition.value)} onChange={(event) => onChange({ ...condition, value: event.target.value })} placeholder={field?.kind === "number" ? "Enter a number" : "Value"} className="h-9 text-xs" />}<button type="button" aria-label="Remove condition" onClick={onRemove} className="flex size-9 shrink-0 items-center justify-center rounded-md text-[var(--text-muted)] hover:bg-red-50 hover:text-[var(--danger)]"><Trash2 size={15} /></button></div>; }
