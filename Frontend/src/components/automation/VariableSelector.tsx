import { useState } from "react";
const variables = ["contact.name", "contact.phone", "contact.email", "contact.company", "conversation.assigned_agent", "message.text"];
export function VariableSelector({ customFields = [], onSelect }: { customFields?: string[]; onSelect: (variable: string) => void }) { const [open, setOpen] = useState(false); const values = [...variables, ...customFields.map((field) => `contact.${field}`)]; return <span className="relative inline-block"><button type="button" onClick={() => setOpen((current) => !current)} className="text-xs font-medium text-[var(--brand)] hover:underline">Insert Variable</button>{open && <div className="absolute bottom-7 left-0 z-30 w-56 rounded-md border border-[var(--border)] bg-white p-1 shadow-[0_8px_25px_rgba(30,40,55,.15)]">{values.map((variable) => <button key={variable} type="button" onClick={() => { onSelect(`{{${variable}}}`); setOpen(false); }} className="block w-full rounded px-2.5 py-2 text-left text-xs hover:bg-[var(--brand-soft)]">{`{{${variable}}}`}</button>)}</div>}</span>; }
